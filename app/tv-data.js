/*
 * tv-data.js — 离线回退快照（产品层空壳）
 *
 * 这是内容层的占位文件，仓库里永远保持为空：
 *   - 正常情况下前端直接读后端 /api/*，这份数据不会被用到；
 *   - 只有后端未启动、或纯静态托管（没有 /api）时，才作为回退渲染源。
 *
 * 如果需要一份可离线打开的演示快照，从本机内容层导出后覆盖此文件即可，
 * 但不要把它提交回仓库——内容层不进 git。
 */
window.TV_DATA = { topics: [], posts: [] };
